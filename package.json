{
  "name": "car-rental-api",
  "version": "1.0.0",
  "description": "JSON Server API for Car Rental Accessories",
  "main": "server.js",
  "scripts": {
    "start": "json-server --watch db.json --port 3000"
  },
  "dependencies": {
    "json-server": "^0.17.0"
  }
}
